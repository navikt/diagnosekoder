name: Publish java and npm packages
concurrency: publish
on:
  workflow_dispatch:

jobs:
  publish-github:
    name: Publish to GitHub Packages
    runs-on: ubuntu-latest
    environment: production
    permissions:
      packages: write
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: 'npm'
          registry-url: "https://npm.pkg.github.com"
          scope: "@navikt"
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
      - run: npm ci --workspace=@navikt/diagnosekoder
      - run: npm run test --workspace=@navikt/diagnosekoder
      - run: npm run build --workspace=@navikt/diagnosekoder

      - name: Setup gradle
        uses: gradle/actions/setup-gradle@v5
      - name: Test java package
        working-directory: java/diagnosekoder
        run: ./gradlew test

      - name: Publish javascript to GitHub Packages
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm publish --workspace=@navikt/diagnosekoder

      - name: Publish java to GitHub Packages
        env:
          GITHUB_USERNAME: x-access-token
          GITHUB_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        working-directory: java/diagnosekoder
        run: ./gradlew publish

  publish-npm:
    name: Publish to npmjs.org
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: 'npm'
          registry-url: "https://registry.npmjs.org"
          scope: "@navikt"
      - run: npm ci --workspace=@navikt/diagnosekoder
      - run: npm run test --workspace=@navikt/diagnosekoder
      - run: npm run build --workspace=@navikt/diagnosekoder
      - name: Publish javascript to npmjs.org
        run: npm publish --workspace=@navikt/diagnosekoder --access public
